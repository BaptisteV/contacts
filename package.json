{
    "name": "contacts",
    "description": "A list of names, stored in a PostgreSQL database",
    "version": "0.0.1",
    "private": true,
    "devDependencies": {
        "mocha": "5.0.0",
        "unit.js": "2.1.1"
    },
    "scripts": {
        "test": "mocha --recursive tests"
    },
    "dependencies": {
        "express": "^4.17.1",
        "pg": "^7.17.0"
    }
}
